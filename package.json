{
  "name": "c-snowload-windregion-cloudflare",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "docker:check": "node scripts/check-docker.mjs",
    "predeploy": "npm run docker:check",
    "predev": "npm run docker:check",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.20"
  },
  "devDependencies": {
    "wrangler": "^4.10.0"
  }
}
